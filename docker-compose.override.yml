services:
  django:
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - tailwind
      - redis
      # - postgres
      # - seaweedfs

  # tailwind:
  #   depends_on:
  #     - postgres

#   postgres:
#     image: postgres:16
#     container_name: postgres-container
#     restart: always
#     environment:
#       POSTGRES_DB: ${POSTGRES_DB}
#       POSTGRES_USER: ${POSTGRES_USER}
#       POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     ports:
#       - "5432:5432"

#   seaweedfs:
#     image: chrislusf/seaweedfs
#     container_name: seaweedfs-container
#     restart: always
#     command: server -master.dir=/data/master -volume.dir.idx=/data/volume -filer
#     volumes:
#       - seaweedfs_data:/data
#     ports:
#       - "8888:8888"
#       - "9333:9333"

# volumes:
#   postgres_data:
#   seaweedfs_data:
